# install vim
sudo apt-get install vim
sudo apt-get install vim-gui-common
sudo apt-get install ack-grep
sudo ln -s /usr/bin/ack-grep /usr/bin/ack


# install and config git
sudo apt-get install git-core
sudo apt-get install gitk
ssh-keygen -t rsa -C "linjian815@gmail.com"
cat ~/.ssh/id_rsa.pub
# copy the key and paste it to github

git config --global user.name "Jian Lin"
git config --global user.email "linjian815@gmail.com"
git config --global github.user linjian
# replace ****** with token
git config --global github.token ******
git config --global merge.tool gvimdiff


# install ruby
sudo apt-get install ruby
sudo apt-get install irb


# update gem
sudo gem install rubygems-update
# gem env to see the path
cd /var/lib/gems/1.8/
sudo ./update_rubygems


# install rails 3
sudo gem install rails


# install and config rvm
sudo apt-get install curl
# vpn
bash < <(curl -s https://rvm.beginrescueend.com/install/rvm)
# Place the folowing line at the end of your shell's loading files, after all PATH/variable settings:
[[ -s "/home/linjian/.rvm/scripts/rvm" ]] && source "/home/linjian/.rvm/scripts/rvm"  # This loads RVM into a shell session.

rvm install ruby-1.8.7
sudo apt-get install libssl-dev
sudo apt-get install libreadline5-dev
rvm install ree
rvm --default use 1.8.7

# to enable readline for ruby-1.8.7
# https://rvm.beginrescueend.com/packages/readline/
rvm notes # to be sure that you have installed any dependencies for the ruby you are installing
cd ~/.rvm/src/ruby-1.8.7/ext/readline
ruby extconf.rb -- --with-readline-dir="~/.rvm/usr"
make install


# install rails and gems
rvm default
# do NOT use sudo
gem install rails -v x.x.x
gem install fastri
gem install mongo_mapper


# install mongodb
# sudo apt-get install mongodb
# http://www.mongodb.org/display/DOCS/Ubuntu+and+Debian+packages
sudo apt-key adv --keyserver keyserver.ubuntu.com --recv 7F0CEB10
# add the following line to /etc/apt/sources.list
deb http://downloads-distro.mongodb.org/repo/ubuntu-upstart dist 10gen
sudo apt-get update
sudo apt-get install mongodb-10gen
sudo mkdir -p /data/db/

